// tailwind.config.ts
import type { Config } from 'tailwindcss'

export default {
  content: [
    './src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {
      // 以前のextend設定はここにコピー
    },
  },
  plugins: [
    // 以前のplugins設定はここにコピー
  ],
} satisfies Config